# TypeScript Examples

Demonstrates type-safe usage patterns.

- **strict-types.ts** — Full type annotations with `ScanResult`, `FileAnalysis`, `FunctionInfo`, etc.
- **builder-pattern.ts** — Fluent builder API with type inference on every chained method

Run any example: `npx tsx examples/04-typescript/strict-types.ts`

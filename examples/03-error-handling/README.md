# Error Handling Examples

Demonstrates how codemap handles errors gracefully.

- **try-catch.ts** — Catch `CodemapError`, `ConfigError`, and `ScanError` by type
- **parser-errors.ts** — Files that fail to parse still appear in results with basic metadata

Run any example: `npx tsx examples/03-error-handling/try-catch.ts`

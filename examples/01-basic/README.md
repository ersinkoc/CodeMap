# Basic Examples

Minimal examples showing core `scan()` usage.

- **minimal.ts** — Scan a directory in 5 lines
- **with-options.ts** — All available `ScanOptions` fields explained
- **multiple-formats.ts** — Generate compact, JSON, and Markdown outputs

Run any example: `npx tsx examples/01-basic/minimal.ts`
